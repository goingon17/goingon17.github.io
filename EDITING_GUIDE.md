# 个人主页编辑指南 (Editing Guide)

你的主页现在采用了极简的单页结构，所有的信息都集中在根目录下的 `index.html` 文件中。你可以通过编辑该文件来更新你的个人资料。

## 1. 基础信息修改
打开 `index.html`，通过搜索以下关键词找到对应位置：

*   **姓名**：搜索 `MINQI GENG` 或 `耿敏祺`。
    *   注意：修改名字时，请同时更新 `data-value="MINQI GENG"` 属性，否则“解密动效”会显示旧名字。
*   **头衔**：搜索 `// Cryptography Researcher & Student`。
*   **所属单位**：搜索 `Affiliation` 下方的文字。
*   **研究方向**：在 `Focus Areas` 区域的 `<span>` 标签中修改。
*   **个人简介**：搜索 `I'm currently developing toward` 修改段落内容。

## 2. 社交链接
在文件末尾的 `<!-- Actions -->` 区域：
*   **GitHub**: 修改 `href="https://github.com/goingon17"`。
*   **Twitter**: 修改 `href="https://twitter.com/goingon0x11"`。
*   **Email**: 修改 `href="mailto:minqigeng@gmail.com"`。

## 3. 更换头像
1.  将你的新头像图片放入 `images/` 文件夹。
2.  在 `index.html` 中搜索 `img src="/images/112929233.jpeg"`，将路径改为你的新图片文件名。

## 4. 进阶设置 (Canvas 背景)
如果你想调整背景流场的速度或颜色，可以找到文件末尾的 `<script>` 部分：
*   **颜色**：搜索 `rgba(0, 243, 255, 0.15)`，修改为你喜欢的颜色（如紫色 `rgba(188, 19, 254, 0.15)`）。
*   **粒子数量**：修改 `for (let i = 0; i < 200; i++)` 中的 `200`。
*   **流动速度**：修改 `time += 0.005` 中的数值。

## 5. 部署
修改完成后，只需执行以下命令并推送到 GitHub 即可：
```bash
git add index.html
git commit -m "Update profile information"
git push
```
---
*Generated by Gemini CLI - 2026*
